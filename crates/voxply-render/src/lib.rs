//! voxply-render — GPU rendering with wgpu
//!
//! Opens a window (via winit), sets up a wgpu rendering pipeline,
//! and runs the main render loop for the game world.

/// Placeholder — will create window and start the render loop
pub fn init() {
    tracing::info!("voxply-render: initialized");
}
