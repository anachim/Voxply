[package]
name = "voxply-voice"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }

# webrtc — pure Rust WebRTC implementation for voice chat
webrtc = "0.12"
